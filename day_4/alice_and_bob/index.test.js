/* 
-> Accepts a list and a number
-> throws error if arguments are in wrong order
-> throws error if arguments are not array and number
-> throws error if array contains non-numbers
-> output should be a list
-> output should have no more than n repeats
-> output should include motifs from input (unless n=0)
 */

const cullList = require("./index");

